{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "curl -sf http://localhost:3000/healthz >/dev/null 2>&1 || (pkill -f 'kubectl.*port-forward.*3000' 2>/dev/null; sleep 1; kubectl port-forward -n comprasweb-local svc/comprasweb 3000:3000 >/dev/null 2>&1 &)",
            "async": true,
            "timeout": 15,
            "statusMessage": "Verificando porta 3000..."
          }
        ]
      }
    ]
  }
}
